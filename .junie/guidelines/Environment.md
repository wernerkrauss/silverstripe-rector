# Entwicklungsumgebung & Troubleshooting (WSL2 / DDEV)

## Shell-Standard
- Alle Befehle müssen in der Bash (WSL2/Ubuntu) ausgeführt werden.
- PHP-Befehle immer via `ddev` (z.B. `ddev php`, `ddev composer`, `ddev phpunit`).


